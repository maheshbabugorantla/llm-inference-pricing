# M05.5a — GCP Scraper (carved from M05.5)

**Status:** Done

**Goal.** Add Google Cloud GPU pricing to the dump/load pipeline introduced in M05.6. Captures on-demand and Committed-Use Discount (CUD) tiers for all GPU families available in `us-central1` via the Cloud Billing Catalog API.

**Depends on.** M05.6 (dump/load pipeline).

**Deferred.** AWS + Azure scrapers → M05.5b (after M06).

---

## Tasks

### T01 — ADR + google-auth dependency
- `docs/adr/0013-google-auth-dep-for-gcp-scraper.md`: justifies `google-auth` over `google-cloud-billing` SDK (1 transitive dep vs ~10); documents `_HttpxTransport` pattern to avoid adding `requests`.
- `pyproject.toml`: added `google-auth>=2.30`; mypy override for `google.*` stubs.
- Commit: `chore(deps): add google-auth for GCP Cloud Billing scraper (ADR-0013)`

### T02 — GPU catalog rows
- `seeds/gpus.yaml`: added `nvidia-t4`, `nvidia-v100`, `nvidia-p100`, `nvidia-p4`.
- `catalog/tests/test_seeds_smoke.py`: updated GPU count 12 → 16, asserted new slugs.
- Commit: `feat(catalog): add T4, V100, P100, P4 GPU entries for GCP coverage`

### T03 — GCP scraper module (fixture-driven TDD)
- `pricing/scrapers/gcp.py`: `map_gcp_gpu`, `parse_gcp_skus`, `fetch_gcp_skus` (paginated, `@retry`), `scrape`.
- `pricing/tests/fixtures/gcp_skus_a3.json`: 18 representative SKUs trimmed from live API (covers on-demand, cud-1yr, cud-3yr, DWS/preemptible/vGPU skip cases).
- `pricing/tests/test_gcp_scraper.py`: 25 fixture-driven tests — zero real network.
- Commit: `feat(scrapers): add gcp cloud-billing scraper with fixture-driven tests`

### T04 — Register in SCRAPERS + provider seed
- `pricing/scrapers/__init__.py`: added `gcp` `ScraperEntry`.
- `seeds/providers.yaml`: added `gcp` provider row (`data_source_tier: realtime_api`).
- `pricing/tests/test_seed_providers.py`: extended slug set to include `"gcp"`.
- Commit: `feat(pipeline): register gcp scraper in SCRAPERS and seed provider row`

### T05 — Credentials documentation
- `pricing/scrapers/README.md`: per-provider auth table; GCP section with IAM setup, local key setup, GH Actions secret pattern.
- Commit: `docs(scrapers): document gcp credentials and local setup`

### T06 — GitHub Actions auth wiring
- `.github/workflows/scrape-pricing.yml`: step writes `GCP_SA_KEY` secret to `${RUNNER_TEMP}/gcp-sa.json` and exports `GOOGLE_APPLICATION_CREDENTIALS`; guarded with `if [ -n "$GCP_SA_KEY" ]` so forks/PRs without the secret skip gracefully.
- GCP service account `pricing-scraper@gcp-cert-prep-429302.iam.gserviceaccount.com` created with `roles/billing.viewer` on billing account `01BF64-98CFC4-22EC68`.
- `GCP_SA_KEY` stored as GitHub Actions repo secret.
- Commits: `ci: wire gcp service-account key into scrape workflow` + `fix(ci): check GCP_SA_KEY in shell instead of step if condition`

### T07 — Spec + INDEX update
- Created this file.
- Updated `spec/INDEX.md`: M05.5a marked done; M05.5 relabelled as M05.5b (AWS+Azure, deferred post-M06).

---

## Scraper output

`data/pricing/gcp.json` — 32 rows across 10 GPU families:

| GPU | on_demand | cud-1yr | cud-3yr |
|---|---|---|---|
| nvidia-h100-sxm-80 | ✓ | ✓ | ✓ |
| nvidia-h200 | — | ✓ | ✓ |
| nvidia-a100-sxm-80 | ✓ | ✓ | ✓ |
| nvidia-a100-sxm-40 | ✓ | ✓ | ✓ |
| nvidia-b200 | — | ✓ | ✓ |
| nvidia-l4 | ✓ | ✓ | ✓ |
| nvidia-t4 | ✓ | ✓ | ✓ |
| nvidia-v100 | ✓ | — | — |
| nvidia-p100 | ✓ | — | — |
| nvidia-p4 | ✓ | — | — |

Region: `us-central1` only. Multi-region deferred to M07.
